import { Credentials } from "./auth.tests";

export const ADMIN_CREDENTIALS: Credentials = {
	login: "admin",
	password: "@admin123",
};

export const NORMAL_CREDENTIALS: Credentials = {
	login: "login",
	password: "12345678",
};
